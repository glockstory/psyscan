import { StyleSheet } from "react-native";

export const chooseTestStyles = StyleSheet.create({
  text: {
    fontSize: 24,
    color: "white",
    textAlign: "center",
    width: "75%",
    marginBottom: 100,
    marginTop: 100,
    minWidth: 150,
  },
});
