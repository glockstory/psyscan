import { StyleSheet } from "react-native";

export const menuPanelStyles = StyleSheet.create({
  title: {
    fontSize: 18,
    color: "#00B9C2",
    textTransform: "uppercase",
    paddingLeft: 10,
    paddingTop: 20,
  },
});
