import { StyleSheet } from "react-native";

export const registrationStyles = StyleSheet.create({
  title: {
    fontSize: 16,
    color: "white",
  },
  content: {
    alignItems: "center",
    marginBottom: 10,
  },
});
